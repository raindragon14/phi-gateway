# AGENTS.md — Phi AI Gateway

> **HANDOFF PROTOCOL v1.0 | 2026-05-11**
> If you are an AI coding agent reading this: everything you need is here.
> Start from this file. Do not ask the human to repeat context.
> Read docs/ in this order: 01-research → 02-architecture → 03-decisions → 04-implementation

---

## PROJECT IDENTITY

```yaml
name: "Phi AI Gateway (φ-Gateway)"
domain: phiconsulting.biz.id
api_subdomain: api.phiconsulting.biz.id
tagline: "Agent-First API Platform"
elevator_pitch: >
  Lightweight API platform that provides all primitives AI agents need
  (LLM proxy, tool registry, knowledge base, agent memory) in one
  docker compose up. Designed for Indonesian developers. Runs on 4GB VPS.
one_liner: "LiteLLM + RAG + Tool Registry + Agent Memory in one API"
```

---

## CURRENT STATE (2026-05-11)

**Phase:** All 4 phases COMPLETE
**Code written:** ~6,500 lines across 35+ source files
**Decision made:** Path A (pure infrastructure layer, no OpenClaw/Hermes dependency)
**Next milestone:** Public launch — VPS deploy + DNS + community outreach

**What has been done:**
- [x] Market research (TAM/SAM/SOM, competitor landscape, pricing benchmarks)
- [x] Architecture design (services, data models, RAM budget)
- [x] Technology selection (FastAPI, SQLite, sqlite-vec, Caddy, MCP)
- [x] Go-to-market strategy (beachhead: Indonesian AI indie hackers + dev agencies)
- [x] Architecture Decision Records (see docs/03-decisions.md)
- [x] Risk assessment (top 3 risks identified with mitigations)
- [x] **Phase 1** (2026-05-11): Scaffold, config, DB, auth, LLM proxy, chat completions, streaming
- [x] **Phase 2** (2026-05-11): Tool registry, MCP endpoint, knowledge base, embeddings, semantic search
- [x] **Phase 3** (2026-05-11): Agent memory, context management, usage analytics, HTMX dashboard
- [x] **Phase 4** (2026-05-11): Landing page, bilingual docs, deployment guide, README, LICENSE, hardening
- [ ] VPS setup + DNS + launch (PENDING)

---

## HARD CONSTRAINTS

```yaml
vps:
  cpu: "2-core Intel Xeon Platinum 8163 @ 2.50GHz (AVX-512 capable)"
  ram: "4 GB (resizable)"
  os: "Ubuntu (minimal)"
  provider: cloud_based

ram_budget_mvp:
  os: "~400 MB"
  caddy: "~50 MB"
  fastapi_2_workers: "~300 MB"
  sqlite: "~20 MB"
  sqlite_vec: "~200 MB"
  docker_overhead: "~200 MB"
  total_base: "~800 MB"
  remaining: "~3.2 GB"
  note: "Do NOT add services that push base beyond 1.5 GB"

non_negotiable:
  - "Must run on 4GB VPS (single machine, docker compose)"
  - "Must self-host (no mandatory cloud dependency)"
  - "Must be open source core (MIT license planned)"
  - "All API responses must be JSON with JSON Schema"
  - "Must support MCP (Model Context Protocol) from day 1"
  - "Documentation must be bilingual: English (primary) + Bahasa Indonesia"
```

---

## TECH STACK (FINAL)

```yaml
backend:
  language: "Python 3.12"
  framework: "FastAPI"
  async: "uvicorn[standard]"
  validation: "Pydantic v2"

database:
  primary: "SQLite (via SQLAlchemy + aiosqlite)"
  vector: "sqlite-vec (zero-dependency SQLite extension)"
  migration: "Alembic"
  note: "Migrate to PostgreSQL only when >100 concurrent users"

protocols:
  api: "REST + JSON (OpenAPI 3.1 auto-generated by FastAPI)"
  agent: "MCP (JSON-RPC 2.0) — agent ↔ tool communication"
  streaming: "SSE (Server-Sent Events)"

reverse_proxy:
  server: "Caddy"
  reason: "Auto SSL via Let's Encrypt, single binary, ~50MB RAM"

containerization:
  tool: "Docker + docker compose"
  services: "api, caddy (2 containers total for MVP)"

llm_integration:
  providers: "OpenAI, Anthropic, Groq (free tier), Ollama (local, optional)"
  proxy_pattern: "OpenAI-compatible format; route by model name"
  fallback: "Auto-failover between providers"
```

---

## WHAT THIS PROJECT IS (AND IS NOT)

### IS:
```yaml
phi_gateway_is:
  - "API platform that provides primitives for AI agent developers"
  - "LLM proxy to multiple providers with unified interface"
  - "Tool registry where agents discover and call tools (MCP-native)"
  - "Knowledge base API for RAG (document ingest + semantic search)"
  - "Agent memory API (session + persistent cross-session)"
  - "Self-hosted, lightweight, runs on small VPS"
  - "Priced in IDR for Indonesian developers"
  - "Open source core with optional managed hosting"
```

### IS NOT:
```yaml
phi_gateway_is_not:
  - "An AI agent itself (it's infrastructure FOR agents)"
  - "A chatbot or personal assistant (that's OpenClaw/Hermes)"
  - "A no-code platform (target users are developers who write code)"
  - "An LLM training/fine-tuning service"
  - "A LangChain/LangGraph competitor (it's infrastructure layer, not framework)"
  - "A consumer product (B2B developer tools)"
  - "OpenAI/Anthropic wrapper with markup (value-add is tool registry + KB + memory)"
```

---

## PROJECT STRUCTURE (PLANNED)

```text
D:/PhiConsulting/
├── AGENTS.md                    # ← YOU ARE HERE
├── docs/
│   ├── 01-research.md           # Market research, competitors, pricing
│   ├── 02-architecture.md       # System design, data models, API contracts
│   ├── 03-decisions.md          # Architecture Decision Records (ADRs)
│   └── 04-implementation.md     # Phased implementation plan
├── src/                         # (TO BE CREATED — Phase 1)
│   └── phi_gateway/
│       ├── main.py
│       ├── config.py
│       ├── api/
│       ├── core/
│       ├── models/
│       └── services/
├── docker-compose.yml           # (TO BE CREATED)
├── Caddyfile                    # (TO BE CREATED)
├── .env.example
└── README.md                    # Human-readable (TO BE CREATED)
```

---

## KEY DECISIONS (TL;DR)

| # | Decision | Why | See |
|---|----------|-----|-----|
| 1 | **Path A — pure infrastructure** | Build own platform, not depend on OpenClaw/Hermes | ADR-001 |
| 2 | **Python + FastAPI** over Go/Rust | Speed of development, AI ecosystem, async native | ADR-002 |
| 3 | **SQLite + sqlite-vec** over PostgreSQL + ChromaDB | Zero ops, <200MB, 35x faster than pgvector | ADR-003 |
| 4 | **Caddy** over Nginx | Auto SSL, simpler config, single binary | ADR-004 |
| 5 | **Proxy-first** (not local LLM) | 4GB constraint, better to proxy Groq/OpenAI | ADR-005 |
| 6 | **MCP protocol** from day 1 | Becoming de facto standard for agent-tool communication | ADR-006 |
| 7 | **Beachhead: Indo AI devs** | Underserved, no local competitor, IDR pricing advantage | ADR-007 |

---

## IMMEDIATE NEXT ACTIONS (FOR NEXT AGENT SESSION)

```yaml
priority_1:
  action: "Deploy to production VPS"
  steps:
    - "Provision Ubuntu 24.04 VPS (4GB RAM)"
    - "Install Docker + docker compose"
    - "Clone repo, configure .env"
    - "docker compose up -d"
    - "Point DNS → VPS IP"
    - "Verify Caddy auto-SSL"
  phase: launch

priority_2:
  action: "Add real API key for a provider and run full E2E test"
  verify: "curl /v1/chat/completions returns valid response from a real LLM"
  phase: launch

priority_3:
  action: "Community launch"
  tasks:
    - "Post in Indonesian AI Telegram/Discord groups"
    - "Publish on GitHub (public repo)"
    - "Share on Twitter/X dev circles"
  phase: post-launch
```

---

## AGENT INSTRUCTIONS

When implementing this project:

1. **Read docs/ in order** before writing any code
2. **Respect RAM budget** — every new dependency must be justified against the 4GB constraint
3. **MCP-native** — every tool endpoint must expose a JSON Schema definition
4. **OpenAI-compatible** — LLM proxy must accept/produce OpenAI chat format
5. **OpenAPI 3.1** — FastAPI auto-generates it; ensure it's complete and accurate
6. **Test from day 1** — write tests alongside implementation (pytest + httpx)
7. **Commit messages in English** — conventional commits format
8. **AGENTS.md is truth** — if implementation diverges from docs, update BOTH
9. **When uncertain** — refer to docs/03-decisions.md for rationale, then ask human

---

## HUMAN CONTACT

Project owner speaks: **Bahasa Indonesia** (native), English (technical)
Communication style: Direct, technical, prefers structured responses
Time zone: Indonesian (WIB/WITA/WIT — likely WIB, UTC+7)

---

*End of handoff. Proceed to docs/01-research.md for market context.*
