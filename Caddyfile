# Note: This is the dev Caddyfile for docker compose local dev.
# Production config lives at /home/reihan/docker/caddy/Caddyfile on the VPS.

api.phiconsulting.biz.id {
    reverse_proxy api:8000
}

staging.phiconsulting.biz.id {
    reverse_proxy api:8000
}

phiconsulting.biz.id {
    reverse_proxy api:8000
}

# Local development
localhost {
    reverse_proxy api:8000
}
