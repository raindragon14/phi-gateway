# Add this to your main Caddyfile at /opt/docker/caddy/Caddyfile
# The phi-api container runs on the caddy_net Docker network

api.phiconsulting.biz.id {
    reverse_proxy phi-api:8000
}

staging.phiconsulting.biz.id {
    reverse_proxy phi-api:8000
}

phiconsulting.biz.id {
    reverse_proxy phi-api:8000
}
